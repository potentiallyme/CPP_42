#ifndef BAINUR_HPP
# define BAINUR_HPP

# include <iostream>
# include <string>

class BainUr
{
	public:
		// Constructors
		BainUr();
		BainUr(const BainUr &copy);
		
		// Destructor
		~BainUr();
		
		// Operators
		BainUr & operator=(const BainUr &assign);
		
	private:
		
};

#endif
