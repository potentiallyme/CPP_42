#ifndef CURE_HPP
# define CURE_HPP

#include <iostream>
#include <string>
#include "Macros.hpp"

class Cure
{
	public:
		// * Constructors
		Cure();
		Cure(const Cure &);

		// * Destructor
		~Cure();

		// * Operator(s)
		Cure & operator=(const Cure &);
		
		// * Getters / Setters

	private:
		
};

#endif
